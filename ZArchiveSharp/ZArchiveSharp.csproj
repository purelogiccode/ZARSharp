<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFrameworks>net8.0;net9.0;net10.0</TargetFrameworks>
        <LangVersion>14</LangVersion>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>ZArchiveSharp</RootNamespace>

        <!-- NuGet metadata -->
        <PackageId>ZArchiveSharp</PackageId>
        <!-- Version is derived from git tags via MinVer. Tag format: v1.0.0
             (v-prefix required; see MinVerTagPrefix in Directory.Build.props). -->
        <Title>ZArchiveSharp</Title>
        <Description>Pure-C# port of the ZArchive 0.1.2 library: directory-tree archives with per-block zstd compression. Includes a dependency-free RFC 8878 zstd encoder and decoder. Trimmable and AOT-compatible.</Description>
        <PackageDescription>Pure-C# port of the ZArchive 0.1.2 library: directory-tree archives with per-block zstd compression (levels 1-6, raw fallback for incompressible blocks). Includes a dependency-free RFC 8878 zstd encoder and decoder. BCL-only, trimmable, and AOT-compatible.</PackageDescription>
        <PackageTags>zarchive;zar;zstd;compression;archive</PackageTags>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageIcon>zar.png</PackageIcon>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <MinClientVersion>5.0</MinClientVersion>

        <!-- XML documentation -->
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <TreatWarningsAsErrors>true</TreatWarningsAsErrors>

        <!-- Trimming and AOT compatibility -->
        <IsTrimmable>true</IsTrimmable>
        <IsAotCompatible>true</IsAotCompatible>

        <!-- Source Link + reproducible builds -->
        <PublishRepositoryUrl>true</PublishRepositoryUrl>
        <EmbedUntrackedSources>true</EmbedUntrackedSources>
        <DebugType>portable</DebugType>
        <Deterministic>true</Deterministic>
        <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>

        <!-- Package output -->
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <IncludeSymbols>true</IncludeSymbols>
        <SymbolPackageFormat>snupkg</SymbolPackageFormat>

        <!-- API compatibility validation across target frameworks -->
        <EnablePackageValidation>true</EnablePackageValidation>
        <EnableStrictModeForPackageValidation>true</EnableStrictModeForPackageValidation>
    </PropertyGroup>

    <ItemGroup>
        <InternalsVisibleTo Include="ZArchiveSharp.Tests" />
    </ItemGroup>

    <ItemGroup>
        <None Include="README.md" Pack="true" PackagePath="\" />
        <None Include="icon\zar.png" Pack="true" PackagePath="\" />
        <None Include="..\LICENSE" Pack="true" PackagePath="\" />
    </ItemGroup>

    <!-- Source Link + versioning -->
    <ItemGroup>
        <PackageReference Include="Meziantou.Analyzer" Version="3.0.235">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Microsoft.SourceLink.GitHub" PrivateAssets="All" Version="10.0.401" />
        <PackageReference Include="MinVer" PrivateAssets="all" Version="8.0.0">
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Roslynator.Analyzers" Version="5.0.0">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Roslynator.CodeAnalysis.Analyzers" Version="5.0.0">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Roslynator.Formatting.Analyzers" Version="5.0.0">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
    </ItemGroup>

</Project>